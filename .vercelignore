# Vercel only builds the frontend (client/). Ignore everything else so the
# build never trips over the server/ workspace or root configs.
server/
*.log
.git
.gitignore
README.md
DEPLOYMENT.md
docker-compose.yml
render.yaml
nginx/
brand/
